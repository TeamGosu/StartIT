<%= form_for(@purchase) do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% @purchase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :productquantityid %><br />
    <%= f.number_field :productquantityid %>
  </div>
  <div class="field">
    <%= f.label :customerid %><br />
    <%= f.number_field :customerid %>
  </div>
  <div class="field">
    <%= f.label :purchasedate %><br />
    <%= f.text_field :purchasedate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
